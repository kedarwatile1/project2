pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/kedarwatile1/project2.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage'
            }
        }
    }
}
